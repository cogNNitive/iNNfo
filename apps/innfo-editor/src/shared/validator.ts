/**
 * App re-export — validator logic now lives in @cogNNitive/cogNNitive-core.
 * This file preserves import paths for existing app code.
 */
import { validateFormatContent } from '@cogNNitive/cogNNitive-core'
export { validateFormatContent }
