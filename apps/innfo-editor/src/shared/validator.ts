/**
 * App re-export — validator logic now lives in @cognnitive/innfo-core.
 * This file preserves import paths for existing app code.
 */
import { validateFormatContent } from '@cognnitive/innfo-core'
export { validateFormatContent }
