/**
 * App re-export — recursiveParser now lives in @cognnitive/innfo-core.
 * This file preserves import paths for existing app code.
 */
export type { ParseIssue, RecursiveParseResult } from '@cognnitive/innfo-core'
export { recursiveParse } from '@cognnitive/innfo-core'
