/**
 * App re-export — recursiveParser now lives in @cogNNitive/cogNNitive-core.
 * This file preserves import paths for existing app code.
 */
export type { ParseIssue, RecursiveParseResult } from '@cogNNitive/cogNNitive-core'
export { recursiveParse } from '@cogNNitive/cogNNitive-core'
