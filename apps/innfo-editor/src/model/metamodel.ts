/**
 * App re-export — metamodel resolution now lives in @cogNNitive/cogNNitive-core.
 * This file preserves import paths for existing app code.
 */
export { resolveEffectiveMetamodel } from '@cogNNitive/cogNNitive-core'
